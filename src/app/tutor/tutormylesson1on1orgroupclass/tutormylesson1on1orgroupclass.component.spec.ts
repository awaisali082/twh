import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Tutormylesson1on1orgroupclassComponent } from './tutormylesson1on1orgroupclass.component';

describe('Tutormylesson1on1orgroupclassComponent', () => {
  let component: Tutormylesson1on1orgroupclassComponent;
  let fixture: ComponentFixture<Tutormylesson1on1orgroupclassComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Tutormylesson1on1orgroupclassComponent]
    });
    fixture = TestBed.createComponent(Tutormylesson1on1orgroupclassComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
